{/*
 Write a program to return the difference between the count of odd numbers and even numbers.   
 
    public class OddEvenDifference {

    // Function to return the difference between count of odd and even numbers
    public static int findDifference(int n, int[] arr) {
        int oddCount = 0;  // Counter for odd numbers
        int evenCount = 0; // Counter for even numbers
        
        // Loop through the array to count odd and even numbers
        for (int num : arr) {
            if (num % 2 == 0) {
                evenCount++;  // Increment even count if number is even
            } else {
                oddCount++;   // Increment odd count if number is odd
            }
        }
        
        // Return the difference between odd and even counts
        return oddCount - evenCount;
    }

    // Example usage
    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4, 5, 6, 
        ..:.::::::.::::::.:::.::::::::::::::::::::::::::.:::.::.:.:::::::.
        };  // Example array
        int n = arr.length;  // Length of the array
        
        int result = findDifference(n, arr);
        System.out.println("The difference between the count of odd and even numbers is: " + result);
    }
}

*/}